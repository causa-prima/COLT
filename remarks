needed python packages (installed via pip):
- cassandra-driver
- futures (dependency of cassandra-driver)
- six (dependency of cassandra-driver)
- PyYAML
- bitarray
- pyjudy (needs installed judy C-library (?))

- blist (used by cassandra-driver, optional)